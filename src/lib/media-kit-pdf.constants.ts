/** Single-page PDF canvas width; keep in sync with buildShareHtml @page size. */
export const MEDIA_KIT_PDF_WIDTH_PX = 794;

/** Tall single page so native print does not split sections across pages. */
export const MEDIA_KIT_PDF_SINGLE_PAGE_HEIGHT_PX = 20000;
